[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-v --tb=short --strict-markers"

[tool.coverage.run]
source = ["report_config", "report_data", "report_charts", "report_html", "report_ai", "common"]
omit = ["tests/*"]
